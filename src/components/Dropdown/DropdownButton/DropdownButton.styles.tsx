import styled from 'styled-components';
import { BorderProps } from 'styled-system';

type DataProps = {

};

type Props = BorderProps & DataProps;

export const DropdownButtonWrapper = styled.li<Props>`

`;
